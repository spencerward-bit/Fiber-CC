{
  "name": "color-optics",
  "private": true,
  "type": "module",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "cap:sync": "npm run mobile:prepare && npx cap sync",
    "cap:copy": "npm run mobile:prepare && npx cap copy",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1",
    "stripe": "^18.2.0"
  }
}
